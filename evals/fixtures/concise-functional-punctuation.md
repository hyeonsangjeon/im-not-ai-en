# Concise technical: functional punctuation no-op

> Synthetic evaluation fixture. The endpoint, command, ratio, header, and schedule are fictional and were not derived from production systems or workplace data.

- Profile: `concise`
- Register: `technical`

## Source

~~~text
At 10:30, run `deploy --dry-run` against https://example.test/a:b and keep the 3:1 ratio. One thing matters: don't guess. Keep `Retry-After` unchanged.

- Run `npm test`.
- Keep the `X-Trace-ID` header.
~~~

## Immutable spans

- `10:30`
- `` `deploy --dry-run` ``
- `https://example.test/a:b`
- `3:1 ratio`
- `One thing matters: don't guess.`
- `` `Retry-After` ``
- `` `npm test` ``
- `` `X-Trace-ID` ``

## Targeted copyedits

- None. Every colon, hyphen, backtick, apostrophe, and list marker is natural or functional. Return the passage unchanged.

## Assertions

- **Fidelity:** Preserve the exact schedule, command, endpoint, ratio, warning, header, and two-item procedure.
- **Naturalness:** Recognize that reducing the symbol count would make this technical instruction less exact or needlessly change an already natural sentence.
- **Register:** Keep the concise technical instruction copy-ready.
- **Voice preservation:** Keep the natural colon in the warning and its straight apostrophe.
- **Non-invention:** Add no deployment result, environment, explanation, owner, or extra step.

## Common failures

- Remove a Markdown bullet, code span, command flag, URL colon, time colon, ratio colon, or lexical hyphen.
- Rewrite the natural warning merely because it contains a colon.
- Convert the straight apostrophe, add a heading, or append an editorial note.
